name: PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

# Write to gh-pages branch and post comments on the PR
permissions:
  contents: write
  pull-requests: write

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Hugo
        if: github.event.action != 'closed'
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.155.3'
          extended: true

      - name: Build preview
        if: github.event.action != 'closed'
        # -D includes drafts so in-progress posts are visible in the preview
        env:
          PR_NUMBER: ${{ github.event.number }}
        run: hugo --source site --baseURL "https://matfacer.com/pr-preview/pr-${PR_NUMBER}/" -D

      - name: Deploy / remove preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: site/public
          preview-branch: gh-pages
          umbrella-dir: pr-preview
          action: auto
          token: ${{ secrets.GITHUB_TOKEN }}
